@model Victuz.Models.ActivityDetailsViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>ActivityModel</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Activity.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Activity.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Activity.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Activity.Description)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Activity.DateTime)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Activity.DateTime)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Activity.MaxParticipants)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Activity.MaxParticipants)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Activity.CategoryId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Activity.CategoryId)
        </dd>

        <dt class="col-sm-2">
            Beschikbare plekken
        </dt>
        <dd class="col-sm-10">
            @Model.AvailableSpots
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Activity?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>

@* Voor de inschrijf- en uitschrijfknoppen *@
@{
    // Controleer of de gebruiker ingelogd is en de rol "Member" heeft
    var isMember = User.Identity.IsAuthenticated && User.IsInRole("Member");
}

@if (isMember)
{
    if (Model.IsRegistered)
    {
        <form asp-action="Unregister" asp-controller="Registrations" method="post">
            <input type="hidden" name="activityId" value="@Model.Activity?.Id" />
            <button type="submit" style="background-color: red; color: white;">Uitschrijven</button>
        </form>
    }
    else if (Model.AvailableSpots > 0)
    {
        <form asp-action="Register" asp-controller="Registrations" method="post">
            <input type="hidden" name="activityId" value="@Model.Activity?.Id" />
            <button type="submit" style="background-color: blue; color: white;">Inschrijven</button>
        </form>
    }
    else
    {
        <p>Er zijn geen beschikbare plekken meer voor deze activiteit.</p>
    }
}
else
{
    <p>Je moet ingelogd zijn als Member om je in te schrijven voor deze activiteit.</p>
}

@if (TempData["Message"] != null)
{
    <div class="alert alert-success">
        @TempData["Message"]
    </div>
}
